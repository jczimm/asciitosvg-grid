#!/usr/bin/env node
const getStdin = require('get-stdin');
const { getSvg } = require('..');

const input = process.stdin;
if (!input) throw new Error('No input provided');

getStdin().then(getSvg).then(o => console.log(o));